dist/
node_modules/
coverage/
package-lock.json
*.svg
# Leader-owned files — rotated via jq/workflow recipes, not formatter.
progress/
# Auto-generated by `npm run openapi:generate`. Prettier's style
# (single quotes, 2-space indent) conflicts with openapi-typescript's
# output (double quotes, 4-space indent); reformatting breaks codegen
# idempotency. Treat the directory as machine-owned.
src/api/generated/
# Snapshot of the backend's OpenAPI document. Source of truth lives
# in the backend; we treat it as a fetched artefact, not a hand-edited
# file.
openapi.json
